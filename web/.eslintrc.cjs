// Legacy ESLint config stub to complement flat config (eslint.config.js)
// ESLint will use eslint.config.js by default; this file ensures compatibility with tools expecting .eslintrc.*
module.exports = {
  root: true,
  ignorePatterns: ['dist', 'node_modules'],
};